name: Staging Container Build
on:
  workflow_dispatch:
  push:
    branches: [develop]
permissions: {contents: read}
jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: docker compose build
