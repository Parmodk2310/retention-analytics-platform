# Contributing

Create a feature branch, add tests with each behavioral change, run `make lint` and `make test`, and keep migrations backward-compatible. Never commit generated secrets, raw PII, or trained model binaries.
