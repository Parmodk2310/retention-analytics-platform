import {Area,AreaChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {Card} from '@/components/ui/Card';import type {ActivityPoint} from '@/types/api';export function ActivityChart({data}:{data:ActivityPoint[]}){return <Card className="p-5"><div className="mb-4"><h3 className="font-semibold">Active users</h3><p className="text-sm opacity-60">Daily distinct users for the selected window</p></div><div className="h-72"><ResponsiveContainer><AreaChart data={data}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="date" tick={{fontSize:11}} minTickGap={35}/><YAxis tick={{fontSize:11}} width={45}/><Tooltip/><Area type="monotone" dataKey="active_users" fill="hsl(var(--primary)/.15)" stroke="hsl(var(--primary))" strokeWidth={2}/></AreaChart></ResponsiveContainer></div></Card>}
