import {AlertTriangle} from 'lucide-react';import {Button} from './Button';export function ErrorState({message='Could not load data',onRetry}:{message?:string;onRetry?:()=>void}){return <div className="flex min-h-48 flex-col items-center justify-center gap-3 rounded-xl border border-dashed p-8 text-center"><AlertTriangle className="h-6 w-6 text-danger"/><div><p className="font-semibold">{message}</p><p className="text-sm opacity-60">The request failed or timed out.</p></div>{onRetry&&<Button onClick={onRetry}>Retry</Button>}</div>}
