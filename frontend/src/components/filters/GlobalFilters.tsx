import {useUIStore} from '@/store/uiStore';import {CHANNELS} from '@/lib/constants';export function GlobalFilters(){const {days,channel,setDays,setChannel}=useUIStore();const cls='rounded-xl border bg-card px-3 py-2 text-sm';return <div className="flex flex-wrap gap-2"><select className={cls} value={days} onChange={e=>setDays(Number(e.target.value))}><option value={7}>Last 7 days</option><option value={30}>Last 30 days</option><option value={90}>Last 90 days</option><option value={365}>Last 12 months</option></select><select className={cls} value={channel||''} onChange={e=>setChannel(e.target.value||null)}><option value="">All channels</option>{CHANNELS.map(c=><option key={c} value={c}>{c.replace('_',' ')}</option>)}</select><span className="rounded-xl border bg-card px-3 py-2 text-xs opacity-60">UTC · API-backed</span></div>}
