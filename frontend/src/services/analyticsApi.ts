import {api} from './api';
export const analyticsApi={overview:async(days=30)=>(await api.get('/analytics/overview',{params:{days}})).data,activity:async(days=30)=>(await api.get('/analytics/activity',{params:{days}})).data,funnel:async(days=30,channel?:string|null)=>(await api.get('/analytics/funnel',{params:{days,channel:channel||undefined}})).data,retention:async(months=12,channel?:string|null)=>(await api.get('/analytics/retention',{params:{months,channel:channel||undefined}})).data,revenue:async(months=12)=>(await api.get('/analytics/revenue',{params:{months}})).data,channels:async(days=30)=>(await api.get('/analytics/channels',{params:{days}})).data}
